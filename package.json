{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "nodejs  it is an env to run  js code outside the browser also",
  "license": "ISC",
  "author": "noman",
  "type": "commonjs",
  "main": "hello.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " node hello.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "url": "^0.11.4"
  }
}
